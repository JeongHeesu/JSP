<?xml version="1.0" encoding="UTF-8"?>
<!-- 위의 선언문 밑에 주석이 있어야 함 -->
<!-- DB 쿼리문                       -->
<!-- << 개정이력(Modification Information) >>            -->
<!--   수정일          수정자          수정내용              -->
<!--  =======      =======     =====================     -->
<!--  2018.03.09.   정희수          최초작성                 -->
<!-- Copyright (c) 2018 by DDIT  All right reserved      -->
<!DOCTYPE sqlMap    
    PUBLIC "-//ibatis.apache.org//DTD SQL Map  2.0//EN"      
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">
<sqlMap namespace="free">
	<typeAlias alias="freeVO" type="kr.or.ddit.vo.FreeboardVO"/>
	
	<sql id="selectAll">
		SELECT *
		  FROM FREEBOARD
		 WHERE BO_STATUS = 'y'
	</sql>
	
	<select id="freeboardList" resultClass="freeVO">
		SELECT B. *
		   FROM (SELECT ROWNUM rnum, A.*
				   FROM (<include refid="selectAll"/>) A) B
	  ORDER BY B.rnum DESC
	</select>
	
	<insert id="insertFreeboard" parameterClass="freeVO">
		<selectKey keyProperty="bo_no" resultClass="string">
			SELECT freeboard_seq.nextval FROM DUAL
		</selectKey>
		INSERT INTO FREEBOARD
		(
			BO_NO,
			BO_TITLE,
			BO_WRITER,
			BO_NICKNAME,
			BO_PWD,
			BO_MAIL,
			BO_IP,
			BO_CONTENT,
			BO_GROUP
		)
		VALUES
		(
			#bo_no#,
			#bo_title#,
			#bo_writer#,
			#bo_nickname#,
			#bo_pwd#,
			#bo_mail#,
			#bo_ip#,
			#bo_content#,
			#bo_no#
		)
	</insert>
	
	<select id="freeboardInfo" resultClass="freeVO" parameterClass="map">
		<include refid="selectAll"/>
		AND BO_NO = #bo_no#
	</select>
	
	<update id="updateFreeboard" parameterClass="freeVO">
		UPDATE  FREEBOARD
		   SET  BO_TITLE = #bo_title#,
				BO_WRITER = #bo_writer#,
				BO_NICKNAME = #bo_nickname#,
				BO_PWD = #bo_pwd#,
				BO_MAIL = #bo_mail#,
				BO_IP = #bo_ip#,
				BO_CONTENT = #bo_content#
		 WHERE  BO_NO = #bo_no# 
	</update>
	
	<update id="deleteFreeboard" parameterClass="map">
		update freeboard
		   set BO_STATUS = 'n'
		 where BO_NO = #bo_no# 
	</update>
	
</sqlMap>